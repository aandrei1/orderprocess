
services:
  # Learning project: mount the source live so code changes show up in the
  # container without rebuilding the image.
  php:
    volumes:
      - .:/app
      # keep var/ inside the container to avoid host permission clashes on cache/log
      - /app/var/
